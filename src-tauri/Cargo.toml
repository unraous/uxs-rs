[package]
name = "uxuescript"
version = "0.1.0"
description = """
    A xuexitong course-dealing application written in Tauri, Old vision is written in Pyside6, 
    but now I rewrite it in Rust to make it more efficient and cross-platform.
"""
authors = ["unraous"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "uxs_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
tauri = { version = "2.11.5", features = [
    "unstable",
    "image-ico",
    "image-png",
] }
tauri-plugin-opener = "2.5.4"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
log = { version = "0.4.33", features = ["serde"] }
tokio = { version = "1.53.1", features = ["full"] }
tokio-tungstenite = "0.30.0"
futures-util = "0.3.33"
anyhow = "1.0.104"
chrono = "0.4.45"
fern = { version = "0.7.1", features = ["colored"] }
toml = "1.1.3"
scraper = "0.27.0"
regex = "1.13.1"
base64 = "0.22.1"
image = "0.25.10"
fontdue = "0.9.3"
ttf-parser = "0.25.1"
async-trait = "0.1.91"
reqwest = { version = "0.13.4", features = ["json"] }
dotenv = "0.15.0"
strum = { version = "0.28.0", features = ["derive"] }
auto_handler = { path = "./crates/auto_handler" }
bytemuck = "1.25.2"
parking_lot = { version = "0.12.5", features = ["serde"] }
tract = "0.23.4"


[workspace]
members = ["crates/auto_handler"]


# 降低 debug 调试信息级别（显著减少磁盘 I/O 和链接体积）
[profile.dev]
debug = 1
codegen-units = 256

# 第三方依赖开启 3 级优化（依赖库只需编译一次，优化后编译产物更小，加速后续链接）
[profile.dev.package."*"]
opt-level = 3

[profile.release]
opt-level = "s"
lto = "thin"
codegen-units = 1
panic = "abort"
strip = true
